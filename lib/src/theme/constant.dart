enum Service { Lodging, Grooming, Veterinary }

enum ButtonType { TextButton, IconButton, ImageButton, CustomButton }

enum InputType {
  TextInput,
  PasswordInput,
  NumberInput,
  PhoneInput,
  MultilineInput
}
